// API Client - configurable HTTP client for all apps
export { createApiClient } from './client';
export { apiRequest, queryClient, getQueryFn } from './query-client';
export type { ApiConfig } from './types';
